import React from "react";
import { DeleteForeverIcon as CustomDeleteIcon } from "@mui/icons-material";
import IconButton from  "@mui/material"


const DeleteForeverIcon = ({}) => {
  return (
    <IconButton>
      <CustomDeleteIcon />
    </IconButton>
  );
};

export default DeleteForeverIcon;
