import React from "react";
import { CropFreeIcon as CustomCropFreeIcon } from "@mui/icons-material/CropFree";
import IconButton from "@mui/material";

const CropFreeIcon = ({}) => {
  return (
    <IconButton>
      <CustomCropFreeIcon />
    </IconButton>
  );
};

export default CropFreeIcon;
